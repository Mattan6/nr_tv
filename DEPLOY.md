# העלאה לאוויר (Railway)

מדריך להעלאת המערכת לכתובת ציבורית, כך שהטלוויזיה בבית הכנסת והטלפונים של הקהילה
יפתחו את אותה כתובת מכל מקום.

להרצה מקומית לפיתוח ראה `SETUP.md`. המסמך הזה עוסק רק בפרודקשן.

---

## מה בעצם עולה לאוויר

**תהליך אחד, כתובת אחת.** בפרודקשן שרת ה‑Express מגיש גם את הקליינט הבנוי וגם את
ה‑API, על אותו פורט ומאחורי אותה תעודת HTTPS. הקליינט פונה ל‑`/api` יחסי, ולכן אין
שום כתובת או פורט שצריך להגדיר בצד הדפדפן.

| כתובת | מי משתמש |
|---|---|
| `/` | הטלוויזיה (מסך רחב) והטלפונים (מסך נייד) — נבחר לפי גודל החלון |
| `/adminGabbai` | הגבאי, לעריכת התוכן |
| `/zmanim` | מסך זמנים לפי תאריך |

## ⚠️ שני דברים לדעת לפני שמתחילים

**1. אין אימות.** `/adminGabbai` וכל פעולות הכתיבה ב‑API פתוחים לכל מי שמגיע לכתובת.
זו החלטה מודעת. המסקנה המעשית היא הסעיף על גיבויים בסוף המסמך — **אל תדלג עליו**, כי
בלי אימות שחזור מהיר הוא ההגנה היחידה על התוכן.

**2. חובה נפח קבוע (Volume).** כל התוכן — הודעות, שיעורים, מזל טוב, אזכרות, הפס
התחתון וזמני השבת — נשמר בקובץ `content.json` על הדיסק. בלי נפח קבוע הקובץ נמחק
**בכל דיפלוי מחדש**, והתוכן חוזר לערכי ההתחלה. זו לא תקלה נדירה אלא התנהגות ודאית.

מאותה סיבה **אי אפשר** להשתמש ב‑Vercel, Netlify או Cloudflare Workers, וגם לא ב‑free
tier של Render — אין בהם מערכת קבצים קבועה.

---

## שלב 1: יצירת הפרויקט

1. היכנס ל‑[railway.app](https://railway.app) והתחבר עם חשבון GitHub.
2. **New Project → Deploy from GitHub repo** ובחר את `nr_tv`.
3. Railway יזהה את `railway.json` ויריץ את הבנייה לבד. הבנייה הראשונה תיכשל או תעלה
   בלי תוכן קבוע — זה תקין, שלב 2 מתקן את זה.

## שלב 2: הנפח הקבוע — השלב הקריטי

1. בפרויקט ב‑Railway לחץ **`Ctrl+K`** (במק: `⌘K`) כדי לפתוח את לוח הפקודות, והקלד
   `volume`. לחלופין: **קליק ימני על הקנבס של הפרויקט**.
   *(אין מסך `Settings → Volumes` — יצירת נפח היא פעולה בלוח הפקודות.)*
2. בחר את השירות שאליו הנפח יתחבר.
3. **Mount path:** `/data` — נתיב מוחלט, לא יחסי.
4. הגודל הקטן ביותר שמוצע יספיק בגדול; התוכן הוא קובץ טקסט של כמה עשרות קילובייטים.
   (במסלול החינמי התקרה היא 0.5GB, ב‑Hobby 5GB.)

**למה דווקא `/app` לא מופיע כאן:** Railway שם את קוד האפליקציה ב‑`/app`, ואפליקציות
שכותבות לנתיב יחסי כמו `./data` חייבות לעגן ל‑`/app/data`. אצלנו זה לא נדרש, כי
`CONTENT_DIR` בשלב הבא מקבל נתיב מוחלט — ולכן `/data` נקי ועובד.

**שים לב:** הנפח מחובר רק בזמן ריצה, לא בזמן הבנייה. זה תקין עבורנו, כי התוכן נכתב
רק כשהשרת רץ.

## שלב 3: משתני סביבה

**Variables** בשירות:

| משתנה | ערך | חובה |
|---|---|---|
| `CONTENT_DIR` | `/data` | **כן** — מפנה את התוכן לנפח הקבוע |
| `NODE_ENV` | `production` | מומלץ |
| `PORT` | — | לא; Railway מזריק אותו לבד והשרת קורא אותו |
| `MONGODB_URI` | — | לא; ראה למטה |

`CONTENT_DIR` הוא בדיוק המשתנה ש‑`server/src/store/contentStore.js` קורא כדי להחליט
היכן לשמור. אם הוא לא מוגדר, התוכן נכתב לתוך תיקיית הקוד ונמחק בדיפלוי הבא.

**`NODE_ENV=production` ובניית הלקוח — מלכודת ששווה להכיר.** כש‑`NODE_ENV=production`
מוגדר בסביבת הבנייה, npm מדלג על `devDependencies` **גם ב‑`npm ci`**, ובלי להתלונן.
`vite` הוא devDependency, ולכן בניית הלקוח נופלת על `vite: not found` (קוד 127) אחרי
התקנה שנראית מוצלחת לגמרי — הסימן המסגיר הוא מספר חבילות קטן בלוג (33 במקום 272).
לכן `npm run build` מציין `--include=dev` במפורש בהתקנת הלקוח. חוסר הסימטריה מול השרת
מכוון: השרת רק **רץ** ולכן `--omit=dev` נכון לו, בעוד שהלקוח צריך **להיבנות**.
אל תסירו את הדגל כדי "לחסוך מקום" — הוא היחיד שמבדיל בין בנייה מצליחה לנפילה.

**בדיקה מהירה שהנפח באמת מחובר:** Railway מגדיר לבד את `RAILWAY_VOLUME_MOUNT_PATH`.
אם אתה רואה אותו ברשימת המשתנים והערך שלו `/data` — הנפח מחובר, והוא תואם ל‑`CONTENT_DIR`
שהגדרת. אם המשתנה לא קיים כלל, אין נפח וחזור לשלב 2.

**על MongoDB:** המערכת רצה בכוונה בלי מסד נתונים. בלוג תופיע השורה
`⚠️ MongoDB not connected` — **התעלם ממנה**, היא לא מונעת דבר. אין צורך להוסיף
`MONGODB_URI`.

## שלב 4: כתובת

**Settings → Networking → Generate Domain** נותן כתובת `*.up.railway.app` עם HTTPS.
אפשר לחבר דומיין משלך ב‑**Custom Domain**.

## שלב 5: בדיקה אחרי העלייה

עבור על הרשימה בדפדפן:

- [ ] `/` נפתח במחשב ומציג את הלוח המלא.
- [ ] `/` בטלפון מציג את הגרסה הניידת (טור אחד עם "המניין הבא" למעלה).
- [ ] הזמנים מלאים ולא `--:--` (אם כולם ריקים — Hebcal לא נגיש; ראה פתרון תקלות).
- [ ] `/adminGabbai` נפתח, ורענון של הדף לא מחזיר 404.
- [ ] הוסף הודעה בפאנל, ותוך חצי דקה היא מופיעה ב‑`/` בטלפון ובמחשב.
- [ ] **מבחן הנפח:** לחץ Redeploy ב‑Railway, ואחרי שהשירות עולה ודא שההודעה שהוספת
      **עדיין שם**. אם היא נעלמה — הנפח לא מחובר, חזור לשלב 2. זו הבדיקה החשובה
      ביותר במסמך.

---

## הטלוויזיה בבית הכנסת

1. פתח בדפדפן של הטלוויזיה את הכתובת ושמור אותה כדף הבית.
2. **בטל את חיסכון החשמל וכיבוי המסך האוטומטי** בהגדרות הטלוויזיה — אחרת המסך יכבה
   באמצע היום.
3. אם יש אפשרות, הגדר פתיחה אוטומטית של הדפדפן בעלייה, כדי שהפסקת חשמל לא תדרוש
   טיפול ידני.

הדף מיועד ל‑1920x1080 ומתכווץ לבד לכל גודל אחר. אם הדפדפן המובנה של הטלוויזיה איטי
או מתנתק, מקל Android TV זול פותר את זה ומריץ Chrome מלא.

---

## גיבויים

בלי אימות, האיום המעשי אינו פריצה אלא מחיקה בטעות. הגיבוי הופך "כל התוכן נמחק"
משעה של הקלדה מחדש לדקה של שחזור.

הורדה ידנית של גיבוי — עובד מכל מחשב, בלי גישה לשרת:

```bash
curl -s https://<הכתובת-שלך>/api/content > content-backup-$(date +%F).json
```

שווה להריץ אחרי כל עריכה משמעותית, ולשמור עותק בענן. שחזור מלא דורש כתיבה של הקובץ
בחזרה לנפח (דרך Railway CLI).

---

## פתרון תקלות

**כל הזמנים מציגים `--:--`** — הדפדפן לא הצליח להגיע ל‑Hebcal. הזמנים נטענים
מ‑`hebcal.com` על ידי הדפדפן עצמו, לא על ידי השרת, ולכן זה תלוי בחיבור של המכשיר
המציג. בדוק ב‑Console של הדפדפן; אם יש `ERR_CONNECTION_TIMED_OUT` הבעיה ברשת ולא
בקוד. שים לב שזמני שבת שנקבעו ידנית ב‑**זמני שבת** ימשיכו להופיע גם אז.

**`/adminGabbai` מחזיר 404 אחרי רענון** — הקליינט לא נבנה. ודא שפקודת הבנייה היא
`npm run build` ושהיא הצליחה בלוג.

**התוכן חזר לערכי ההתחלה** — הנפח לא מחובר או ש‑`CONTENT_DIR` לא מוגדר. שלבים 2–3.

**שינוי בפאנל לא מופיע על המסך** — המסך מושך תוכן כל 30 שניות. אם עברה דקה, בדוק
שהפריט מסומן "מוצג" ולא "מוסתר".

---

## בדיקה מקומית של מסלול הפרודקשן

לפני העלייה כדאי להריץ בדיוק את מה שרץ בשרת — תהליך אחד, פורט אחד:

```bash
npm run build
npm start
```

ואז לפתוח `http://localhost:5000`. זה מגיש את הקליינט הבנוי ולא את שרת הפיתוח של
Vite, כך שכל תקלת בנייה או נתיב מתגלה כאן ולא בפרודקשן.
